# >the_collective Environment Configuration
# Copy to .env and customize (this file is .gitignored)

# ==============================================================================
# WINDOWS COMPATIBILITY (PowerShell users)
# ==============================================================================
# If running `docker compose up` from PowerShell (not Git Bash), uncomment:
# HOME=C:/Users/YOUR_USERNAME
#
# This is required because PowerShell doesn't expand ${HOME:-${USERPROFILE}}
# Git Bash users don't need this - $HOME is set automatically.

# ==============================================================================
# OPTIONAL: Custom ports (if 3100/3101 are in use)
# ==============================================================================
# MEMORY_PORT=3100
# GEMINI_PORT=3101

# ==============================================================================
# OPTIONAL: Node environment
# ==============================================================================
# NODE_ENV=production

# ==============================================================================
# Note: GEMINI_API_KEY should be set in .collective/gemini-bridge/.env
# ==============================================================================
